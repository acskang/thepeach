"""
Explicit Django settings package for ThePeach.

Select a concrete settings module via `DJANGO_SETTINGS_MODULE`, for example:

- `project.settings.local` for development
- `project.settings.prod` for production
"""
