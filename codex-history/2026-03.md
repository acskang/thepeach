# Codex History: 2026-03

## 2026-03-17 18:01:51 KST

### User Request Summary

- Define a reusable Django platform architecture for `ThePeach`.
- Design and implement the initial core apps: `accounts`, `common`, and `gateway`.
- Set up base settings, base models, utilities, and JWT authentication.

### Result Summary

- Converted Django settings into a package with base/local structure.
- Added API-first DRF configuration, SimpleJWT, custom user model wiring, standardized JSON response handling, shared exception handling, and request-context logging.
- Implemented `common` platform primitives for base models, responses, renderer, pagination, and health endpoint.
- Implemented `accounts` with a UUID-based custom user model, signup, login, logout, token refresh, token verify, and authenticated profile endpoint.
- Implemented `gateway` as the versioned API root and discovery surface.

### Notable Files Changed

- `project/settings/base.py`
- `project/settings/__init__.py`
- `project/settings/local.py`
- `project/urls.py`
- `common/models.py`
- `common/responses.py`
- `common/exceptions.py`
- `common/renderers.py`
- `common/pagination.py`
- `common/middleware.py`
- `accounts/models.py`
- `accounts/serializers.py`
- `accounts/views.py`
- `accounts/urls.py`
- `gateway/views.py`
- `gateway/urls.py`

### Validation Status

- `python manage.py makemigrations`: passed
- `python manage.py migrate`: passed
- `python manage.py check`: passed
- `python manage.py test`: passed

## 2026-03-17 18:01:51 KST

### User Request Summary

- Continue implementation step by step after the initial platform bootstrap.

### Result Summary

- Added persistent `logs` and `services` apps.
- Implemented durable API request logs, authentication event logs, and system logs.
- Wired request logging persistence into middleware and authentication event persistence into account flows.
- Implemented a service registry model and `/api/v1/services/` discovery endpoint.
- Updated pagination so list endpoints preserve the platform response envelope.

### Notable Files Changed

- `project/settings/base.py`
- `logs/models.py`
- `logs/services.py`
- `logs/admin.py`
- `services/models.py`
- `services/serializers.py`
- `services/views.py`
- `services/urls.py`
- `common/middleware.py`
- `common/pagination.py`
- `accounts/views.py`
- `gateway/views.py`
- `gateway/urls.py`

### Validation Status

- `python manage.py makemigrations logs services`: passed
- `python manage.py migrate`: passed
- `python manage.py check`: passed
- `python manage.py test`: passed

## 2026-03-17 18:01:51 KST

### User Request Summary

- Establish a long-term Codex history system for this project.
- Create `codex-history/` under the project root.
- Store requests and results as cumulative Markdown files using a fixed filename rule.
- Keep a non-deletion, non-cleanup policy for existing history files.
- Add the history policy to `AGENTS.md`.
- Ensure future sessions can reference both `AGENTS.md` and `codex-history/`.

### Result Summary

- Created the project-local `codex-history/` directory.
- Added `AGENTS.md` with mandatory session-start instructions and an append-only history policy.
- Added `codex-history/README.md` documenting the fixed filename rule and append-only behavior.
- Created the first cumulative monthly history file for March 2026 and recorded the completed platform work plus this policy setup.

### Notable Files Changed

- `AGENTS.md`
- `codex-history/README.md`
- `codex-history/2026-03.md`

### Validation Status

- Directory and files created successfully.

## 2026-03-17 18:06:49 KST

### User Request Summary

- Read the project root `agents.md` and the latest files in `codex-history/` to recover prior context.
- Continue from the previous implementation step.
- Record this session in the Codex history using the repository filename rule.

### Result Summary

- Read the canonical agent policy and the latest monthly history before continuing.
- Added a lowercase `agents.md` entrypoint that redirects future case-sensitive lookups to `AGENTS.md`.
- Continued the platform implementation by creating the `media` app as the next core module.
- Implemented a centralized media asset registry for images, reusable logos, and video/embed assets.
- Exposed versioned APIs under `/api/v1/media/` for asset listing/creation, logo listing, video listing, and slug-based detail/update/delete.
- Updated the gateway discovery surface to advertise the new media module and capability.
- Verified the media API response contract so create/detail/update/delete responses follow the platform envelope.
- Appended this session to the existing monthly history file rather than creating an ad hoc filename, per project policy.

### Notable Files Changed

- `agents.md`
- `project/settings/base.py`
- `gateway/urls.py`
- `gateway/views.py`
- `media/models.py`
- `media/serializers.py`
- `media/views.py`
- `media/urls.py`
- `media/admin.py`
- `media/tests.py`
- `media/migrations/0001_initial.py`
- `codex-history/2026-03.md`

### Validation Status

- `python manage.py makemigrations media`: passed
- `python manage.py migrate`: passed
- `python manage.py check`: passed
- `python manage.py test`: passed
- media create smoke check via DRF test client: passed

## 2026-03-17 18:28:40 KST

### User Request Summary

- Extend user personal information with company affiliation, department affiliation, and unique smartphone number.
- Ensure all business data belongs to a company.
- Ensure non-superusers can only see data for their own companies.
- Use departments as the basis for permission management.
- Allow users to belong to multiple companies and multiple departments.

### Result Summary

- Added organization models in `accounts`: `Company`, `Department`, `UserCompanyMembership`, and `UserDepartmentMembership`.
- Extended the user model with `smartphone_number` and enforced uniqueness at both serializer and database levels.
- Exposed company and department affiliations in the user serializer/profile response, and embedded company and department IDs into JWT claims.
- Added reusable tenancy helpers in `common.tenancy` for company-scoped query filtering and department-based access validation.
- Converted `services.ServiceRegistry` and `media.MediaAsset` into company-owned business data models.
- Restricted non-superuser access so business queries are filtered to the user’s own companies.
- Required authenticated access for business list/detail APIs and used department membership as the write-access basis for media asset creation, update, and delete.
- Added data migrations that create a legacy default company and assign pre-existing `media` and `services` records into that company before enforcing non-null company ownership.

### Notable Files Changed

- `common/models.py`
- `common/tenancy.py`
- `common/permissions.py`
- `accounts/models.py`
- `accounts/serializers.py`
- `accounts/admin.py`
- `accounts/tests.py`
- `accounts/migrations/0002_user_smartphone_and_organization_models.py`
- `services/models.py`
- `services/serializers.py`
- `services/views.py`
- `services/admin.py`
- `services/tests.py`
- `services/migrations/0002_company_scope.py`
- `media/models.py`
- `media/serializers.py`
- `media/views.py`
- `media/admin.py`
- `media/tests.py`
- `media/migrations/0002_company_scope.py`

### Validation Status

- `python manage.py migrate`: passed
- `python manage.py check`: passed
- `python manage.py test`: passed

## 2026-03-17 19:18:11 KST

### User Request Summary

- Use the locally installed PostgreSQL server for this project.
- Create and use a project-specific database so it does not conflict with existing databases or tables.

### Result Summary

- Verified the local PostgreSQL server was installed and accessible.
- Created a dedicated project database named `thepeach_db` owned by `cskang`.
- Switched Django settings from SQLite fallback behavior to PostgreSQL-first defaults using the local server.
- Set the default database connection values to `thepeach_db` / `cskang` in project settings while still allowing env override.
- Applied all Django migrations against the new PostgreSQL database.
- Re-ran the full test suite against PostgreSQL to confirm the project works on the intended database engine.

### Notable Files Changed

- `project/settings/base.py`
- `codex-history/2026-03.md`

### Validation Status

- `psql -d postgres -c "CREATE DATABASE thepeach_db OWNER cskang ENCODING 'UTF8';"`: passed
- `python manage.py migrate`: passed
- `python manage.py check`: passed
- `python manage.py test`: passed

## 2026-03-18 13:08:25 KST

### User Request Summary

- Perform a second-pass ThePeach architecture audit after the remediation pass.
- Verify claimed fixes against the current repository state instead of trusting prior reports.
- Re-evaluate production hardening, logging, auth routing, gateway semantics, coupling cleanup, and docs.

### Result Summary

- Verified that `project/settings/prod.py` now exists and that `base.py`, `local.py`, and `prod.py` are materially separated.
- Verified production hardening improvements including strict `ALLOWED_HOSTS`, environment-driven `SECRET_KEY`, proxy SSL header support, secure cookie posture, HSTS, and `/logs`-aware file logging handlers.
- Verified failure-safe DB-backed operational logging via safe log persistence wrappers used by request middleware and gateway/account services.
- Verified `/api/v1/auth/` as the canonical auth namespace, removal of the `/api/v1/accounts/` gateway alias, and explicit failed-login event logging.
- Verified gateway unresolved-target handling now returns standardized explicit failure semantics with HTTP 404 rather than soft success-shaped 200 responses.
- Verified serializer-level cross-app presentation coupling was reduced in `media` and `services`, and that previously ambiguous `common/permissions.py` and placeholder `logs/views.py` were removed.
- Verified production deployment documentation and a minimal requirements manifest were added.
- Identified a remaining high-severity production risk: `project/settings/__init__.py`, `project/wsgi.py`, and `project/asgi.py` still default to `project.settings`, which imports `local.py`; if production forgets to set `DJANGO_SETTINGS_MODULE=project.settings.prod`, the app falls back to development settings.
- Identified a remaining medium-severity gateway/API inconsistency: the gateway manifest advertises `gateway` and `health` modules, but the route resolver serializer only allows `auth`, `media`, and `services` as `module` choices.

### Notable Files Inspected

- `project/settings/base.py`
- `project/settings/local.py`
- `project/settings/prod.py`
- `project/settings/__init__.py`
- `project/wsgi.py`
- `project/asgi.py`
- `common/middleware.py`
- `common/logging.py`
- `logs/services.py`

## 2026-03-20 01:53:58 KST

### User Request Summary

- Investigate why the navbar still shows `Login` and `Sign Up` after login.
- Apply the fix and account for production static deployment requirements.

### Result Summary

- Identified that the navbar auth sync script captured the access token at initial page load.
- Fixed the navbar script so each sync call reads the current `localStorage` token before evaluating auth state.
- Confirmed this resolves the stale-state path triggered after modal login/logout without requiring a full page reload.

### Notable Files Changed

- `templates/partials/navbar.html`
- `codex-history/2026-03.md`

### Validation Status

- `python manage.py check`: passed
- `set -a && . /etc/thepeach/thepeach.env && set +a && export DJANGO_SETTINGS_MODULE=project.settings.prod && /home/cskang/miniconda3/envs/dj5/bin/python manage.py collectstatic --noinput`: passed (`0 static files copied`, template-only change)
- gunicorn restart: blocked by sudo password requirement

## 2026-03-19 18:56:23 KST

### User Request Summary

- Summarize why the `ThePeach` system is needed.
- Explain what problems occur without it.
- Clarify what purpose it should be used for and how it should be used.

### Result Summary

- Read the repository guidance and latest monthly Codex history before responding.
- Reviewed the platform overview and operating docs for architecture, auth, gateway, media, and logging.
- Prepared a Korean summary explaining ThePeach as a shared platform for centralized authentication, application governance, reusable media, integration-ready APIs, and operational visibility.

### Notable Files Changed

- `codex-history/2026-03.md`

### Validation Status

- Documentation review completed from `README.md` and `docs/*.md`.
- `logs/models.py`
- `accounts/services.py`
- `accounts/views.py`
- `accounts/urls.py`
- `accounts/tests.py`
- `gateway/urls.py`
- `gateway/services.py`
- `gateway/views.py`
- `gateway/serializers.py`
- `gateway/tests.py`
- `media/serializers.py`
- `services/serializers.py`
- `docs/production-deployment.md`
- `requirements/base.txt`

### Validation Status

- `python manage.py check`: passed
- `env DJANGO_SETTINGS_MODULE=project.settings.prod DJANGO_DEBUG=false DJANGO_SECRET_KEY=prod-secret DJANGO_ALLOWED_HOSTS=thesysm.com,.thesysm.com DJANGO_CSRF_TRUSTED_ORIGINS=https://thesysm.com,https://app.thesysm.com DJANGO_LOG_DIR=/tmp/thepeach-prod-logs python manage.py check`: passed
- `python manage.py test accounts.tests gateway.tests logs.tests`: blocked by sandbox PostgreSQL socket access (`/var/run/postgresql/.s.PGSQL.5432` operation not permitted), so test execution could not be re-verified in this session

## 2026-03-18 10:57:09 KST

### User Request Summary

- Design and implement the `accounts` JWT + DRF + SimpleJWT + SSO-extensible authentication architecture for ThePeach.
- Keep the current repository structure, use `accounts` as the auth owner, and expose required endpoints under `/api/v1/auth/`.

### Result Summary

- Kept `accounts` as the sole authentication authority and refined the existing custom user/JWT foundation instead of rewriting it.
- Split the user manager into `accounts/managers.py` for a cleaner auth core boundary.
- Added `accounts/services.py` as a small auth-boundary layer for auth event logging and future SSO/OAuth/OIDC extension metadata.
- Refined JWT login and refresh handling in `accounts/views.py` so token issuance is explicit and deterministic under the standard ThePeach response envelope.
- Exposed the primary auth surface under `/api/v1/auth/` through `gateway`, while preserving `/api/v1/accounts/` as a legacy-compatible alias.
- Added an auth root discovery endpoint that documents the auth API surface and marks the module as SSO-ready.
- Kept SimpleJWT rotation and blacklist behavior enabled, preserving logout support through refresh token blacklisting.
- Strengthened tests around signup, login, logout, refresh, current-user lookup, and auth-root discovery.

### Notable Files Changed

- `accounts/managers.py`
- `accounts/services.py`
- `accounts/models.py`
- `accounts/serializers.py`
- `accounts/views.py`
- `accounts/urls.py`
- `accounts/tests.py`
- `gateway/urls.py`
- `gateway/views.py`
- `gateway/tests.py`

### Validation Status

- `python manage.py check`: passed
- `python manage.py test`: passed

## 2026-03-18 15:10:00 KST

### User Request Summary

- Implement a production-ready platform logo asset registration feature with reusable APIs, operator-facing screens, application linkage, validation, logging, and production-safe media handling.

### Result Summary

- Added `PlatformLogoAsset` in `media` as the centralized reusable logo asset model for registered application services.
- Implemented explicit logo upload, list, detail, metadata update, replace, deactivate, and by-application APIs under `/api/v1/media/logos/`.
- Introduced `media/logo_service.py` to keep upload validation, file metadata extraction, path generation, replace logic, and deactivate logic out of views.
- Integrated logos with `services.RegisteredApplication` using stable `application_code` lookup and explicit `usage_type` choices.
- Enforced server-side validation for allowed image types, file size limit, empty files, corrupted files, duplicate `logo_code`, and invalid application references.
- Stored uploaded logo files under `media_files/logos/<application_code>/...` with collision-resistant filenames suitable for Nginx-served media.
- Added administrative helper screens for logo list, upload, and edit under `templates/media/logos/` with dedicated `media/web_views.py` and `media/web_urls.py`.
- Added logo event logging for upload attempts, success, validation failure, update, replace, and deactivate actions.
- Added platform documentation for logo assets and preserved the current ThePeach repository structure instead of introducing the conflicting `apps/` or `config/` layout.

### Notable Files Changed

- `media/models.py`
- `media/serializers.py`
- `media/logo_service.py`
- `media/logo_views.py`
- `media/web_views.py`
- `media/web_urls.py`
- `media/urls.py`
- `media/views.py`
- `media/admin.py`
- `media/tests.py`
- `project/settings/base.py`
- `project/urls.py`
- `templates/base.html`
- `templates/media/logos/upload.html`
- `templates/media/logos/list.html`
- `templates/media/logos/form.html`
- `docs/platform-logo-assets.md`
- `media/migrations/0004_platformlogoasset.py`

### Validation Status

- `python manage.py makemigrations media`: passed
- `python manage.py check`: passed
- `python manage.py migrate`: passed
- `env DJANGO_SETTINGS_MODULE=project.settings.prod DJANGO_DEBUG=false DJANGO_SECRET_KEY=prod-secret DJANGO_ALLOWED_HOSTS=thesysm.com,.thesysm.com DJANGO_CSRF_TRUSTED_ORIGINS=https://thesysm.com,https://app.thesysm.com DJANGO_LOG_DIR=/tmp/thepeach-prod-logs python manage.py check`: passed
- `python manage.py test media.tests gateway.tests services.tests`: passed

## 2026-03-18 15:28:00 KST

### User Request Summary

- Implement the foundational UI layout layer with a working root homepage, shared navigation bar, shared footer, reusable base template, and static CSS.

### Result Summary

- Reworked `templates/base.html` into a reusable platform shell with static CSS, shared navbar include, shared footer include, title block, and content block.
- Added `templates/partials/navbar.html` and `templates/partials/footer.html` as reusable layout fragments for future operator-facing pages.
- Added `templates/home.html` and a lightweight `HomeView` so `/` now resolves to a platform homepage instead of returning 404.
- Updated main URL routing so the homepage is explicit and does not interfere with existing API routes or operator pages.
- Added `static/css/base.css` and configured `STATICFILES_DIRS` so layout styling is handled through static assets rather than inline template CSS.
- Preserved template inheritance for existing pages that already extend `base.html`, including application registry and logo management pages.

### Notable Files Changed

- `templates/base.html`
- `templates/partials/navbar.html`
- `templates/partials/footer.html`
- `templates/home.html`
- `static/css/base.css`
- `common/views.py`
- `project/urls.py`
- `project/settings/base.py`
- `common/tests.py`

### Validation Status

- `python manage.py check`: passed
- `python manage.py test common.tests`: passed

## 2026-03-18 16:25:00 KST

### User Request Summary

- Prepare the existing ThePeach repository for safe Git and GitHub management with repository governance, ignore rules, branch strategy, and GitHub remote setup.

### Result Summary

- Initialized the repository as a Git repository with `main` as the default branch.
- Added a Django/ThePeach-safe `.gitignore` to exclude secrets, local databases, runtime artifacts, media uploads, caches, and editor junk.
- Confirmed that no `.env` files or key files were present and that `db.sqlite3` is now ignored rather than staged.
- Added repository governance documentation in `README.md` and `docs/repository-management.md`.
- Added `requirements/local.txt` and `requirements/prod.txt` to clarify environment-specific dependency entry points.
- Added a lightweight GitHub pull request template under `.github/`.
- Created the initial repository governance commit and configured the GitHub remote `origin` as `git@github.com:acskang/thepeach.git`.

### Notable Files Changed

- `.gitignore`
- `README.md`
- `docs/repository-management.md`
- `requirements/local.txt`
- `requirements/prod.txt`
- `.github/pull_request_template.md`

### Validation Status

- `git status --short --branch --ignored`: passed
- `git commit -m "chore: prepare ThePeach repository governance"`: passed
- `git remote add origin git@github.com:acskang/thepeach.git`: passed

## 2026-03-18 21:55:00 KST

### User Request Summary

- Refactor the over-engineered logo/application asset registry into a simple centralized shared image server with reusable URLs and generic `/api/v1/media/assets/` APIs.

### Result Summary

- Replaced the logo-specific media surface with a generic `SharedMediaAsset` model and `/api/v1/media/assets/` contract.
- Removed application registry coupling, `logo_code`, `usage_type`, and logo-governance requirements from the upload and management flow.
- Added `media/media_asset_service.py` and `media/asset_views.py` so upload validation, checksum generation, deduplication, replace logic, and deactivation stay out of views.
- Stored shared assets under `media_files/shared/images/YYYY/MM/` with generated names and persisted `checksum_sha256` for deterministic duplicate detection.
- Added simple operator-facing asset pages under `templates/media/assets/` and changed web routes from `/logos/...` to `/assets/...`.
- Added migration `0005_sharedmediaasset_refactor` to copy existing logo assets into the new generic model and remove the old logo-specific model.
- Replaced logo documentation with a shared image server guide and documented the `/logos/` to `/assets/` contract change.

### Notable Files Changed

- `media/models.py`
- `media/serializers.py`
- `media/media_asset_service.py`
- `media/asset_views.py`
- `media/urls.py`
- `media/web_views.py`
- `media/web_urls.py`
- `media/admin.py`
- `media/tests.py`
- `media/migrations/0005_sharedmediaasset_refactor.py`
- `templates/media/assets/list.html`
- `templates/media/assets/upload.html`
- `templates/media/assets/form.html`
- `templates/partials/navbar.html`
- `templates/home.html`
- `templates/accounts/home.html`
- `docs/shared-media-server.md`

### Validation Status

- `python manage.py check`: passed
- `python manage.py migrate`: passed
- `env DJANGO_SETTINGS_MODULE=project.settings.prod DJANGO_DEBUG=false DJANGO_SECRET_KEY=prod-secret DJANGO_ALLOWED_HOSTS=thesysm.com,.thesysm.com DJANGO_CSRF_TRUSTED_ORIGINS=https://thesysm.com,https://app.thesysm.com DJANGO_LOG_DIR=/tmp/thepeach-prod-logs python manage.py check`: passed
- `python manage.py test media.tests common.tests`: passed

## 2026-03-19 00:12:00 KST

### User Request Summary

- Upgrade the shallow `/docs/` page into a real documentation hub with markdown-backed detail pages, explicit registry metadata, and optional machine-friendly docs APIs.

### Result Summary

- Replaced the shallow docs page with a registry-driven documentation portal built on `common/docs_registry.py`, `common/docs_service.py`, and `common/docs_views.py`.
- Implemented `/docs/` as a full documentation hub with hero, recommended reading order, featured documents, category sections, full document index, platform concept summaries, production summary, and useful navigation.
- Implemented `/docs/<slug>/` detail pages that render actual markdown files from the repository `docs/` directory through safe slug-to-file mapping.
- Added optional docs APIs under `/api/v1/docs/` and `/api/v1/docs/<slug>/` with the standard ThePeach JSON response envelope.
- Added a deterministic document registry with metadata including slug, title, file path, summary, category, audience, ordering, featured/core flags, related documents, and status labels.
- Added new platform documentation files for overview, architecture principles, structure/conventions, API/gateway standards, logging/operations, and the documentation system itself.
- Added dedicated docs templates and `static/css/docs.css` for a more detailed internal platform portal presentation.
- Added tests for docs hub rendering, valid and invalid slug behavior, markdown rendering, category/featured visibility, API response shape, and path traversal rejection.

### Notable Files Changed

- `common/docs_registry.py`
- `common/docs_service.py`
- `common/docs_views.py`
- `common/tests.py`
- `project/urls.py`
- `gateway/urls.py`
- `templates/docs/index.html`
- `templates/docs/detail.html`
- `templates/docs/partials/hero.html`
- `templates/docs/partials/reading_order.html`
- `templates/docs/partials/category_section.html`
- `templates/docs/partials/document_card.html`
- `templates/docs/partials/document_meta.html`
- `static/css/docs.css`
- `docs/platform-overview.md`
- `docs/architecture-principles.md`
- `docs/project-structure-and-conventions.md`
- `docs/api-and-gateway-standards.md`
- `docs/logging-and-operations.md`
- `docs/platform-documentation-system.md`
- `requirements/base.txt`

### Validation Status

- `python manage.py check`: passed
- `python manage.py test common.tests`: passed

## 2026-03-18 14:27:00 KST

### User Request Summary

- Perform the final PASS / NO-PASS decision audit for the current ThePeach repository.
- Verify whether the previously identified final blockers were actually resolved in code.
- Make a strict production-baseline decision with no benefit of the doubt.

### Result Summary

- Re-verified the runtime entry points and confirmed the startup-safety blocker is still unresolved.
- `project/settings/__init__.py` still imports `local.py`, and `manage.py`, `project/wsgi.py`, and `project/asgi.py` still default `DJANGO_SETTINGS_MODULE` to `project.settings`.
- As a result, if production does not explicitly set `DJANGO_SETTINGS_MODULE=project.settings.prod`, the application can still boot with local development settings.
- Re-verified that production hardening inside `project/settings/prod.py` remains present and materially correct.
- Re-verified that auth integrity remains correct: `/api/v1/auth/` is canonical, failed login logging exists, and auth ownership remains in `accounts`.
- Re-verified that logging remains file-compatible and DB-backed logging remains failure-safe.
- Re-verified that gateway unresolved targets still return standardized 404 responses.
- Re-verified that the gateway manifest/resolver mismatch still remains: the manifest advertises `health` and `gateway`, but the resolver serializer only allows `auth`, `media`, and `services`.
- Final decision: the repository does not qualify for a full PASS yet because the remaining startup-safety issue is production-critical.

### Notable Files Inspected

- `project/settings/__init__.py`
- `project/wsgi.py`
- `project/asgi.py`
- `manage.py`
- `project/settings/base.py`
- `project/settings/prod.py`
- `gateway/services.py`
- `gateway/serializers.py`
- `gateway/views.py`
- `gateway/urls.py`
- `accounts/services.py`
- `accounts/urls.py`
- `common/middleware.py`
- `logs/services.py`
- `docs/production-deployment.md`
- `AGENTS.thepeach.md`

### Validation Status

- File-level code verification completed
- No new command-based runtime validation was necessary beyond source inspection for the final PASS gate

## 2026-03-18 14:33:18 KST

### User Request Summary

- Apply the final targeted remediation needed to clear the FAIL verdict from the final ThePeach PASS decision.
- Remove the remaining production startup safety risk, align the gateway contract fully, and update stale governance/docs.

### Result Summary

- Removed the hidden local-settings alias behavior from `project/settings/__init__.py`; the settings package is now documentation-only and no longer imports `local.py`.
- Changed `manage.py` to default explicitly to `project.settings.local`, keeping developer convenience limited to the CLI entry path.
- Changed `project/wsgi.py` and `project/asgi.py` to fail loudly unless `DJANGO_SETTINGS_MODULE` is explicitly set, preventing production from accidentally booting local settings.
- Aligned the gateway resolver contract with the advertised manifest by allowing `auth`, `health`, `media`, `services`, and `gateway` module resolution in `gateway/serializers.py`.
- Removed the stale `services`-specific serializer restriction so the resolver can resolve the `/api/v1/services/` module root consistently with the manifest.
- Updated `AGENTS.thepeach.md` to reflect the real current structure, including `project/settings/prod.py`, and documented explicit local vs production settings usage.
- Updated deployment docs to state that production runtime must set `DJANGO_SETTINGS_MODULE=project.settings.prod`, while `manage.py` uses `project.settings.local` for local development.
- Added focused regression tests for startup safety and gateway contract alignment.

### Notable Files Changed

- `project/settings/__init__.py`
- `project/wsgi.py`
- `project/asgi.py`
- `manage.py`
- `gateway/serializers.py`
- `gateway/tests.py`
- `common/tests.py`
- `AGENTS.thepeach.md`
- `docs/production-deployment.md`

### Validation Status

- `python manage.py check`: passed
- `env DJANGO_SETTINGS_MODULE=project.settings.prod DJANGO_DEBUG=false DJANGO_SECRET_KEY=prod-secret DJANGO_ALLOWED_HOSTS=thesysm.com,.thesysm.com DJANGO_CSRF_TRUSTED_ORIGINS=https://thesysm.com,https://app.thesysm.com DJANGO_LOG_DIR=/tmp/thepeach-prod-logs python manage.py check`: passed
- explicit import validation for `project.wsgi` and `project.asgi` without `DJANGO_SETTINGS_MODULE`: passed
- `python manage.py test common.tests gateway.tests`: passed

## 2026-03-18 15:22:51 KST

### User Request Summary

- Implement a production-ready centralized authentication and application registry foundation for ThePeach.
- Add signup/login/logout screens and APIs, preserve centralized JWT auth in `accounts`, and introduce platform-governed application/screen/feature registration under `services`.
- Prepare gateway integration surfaces for future trusted apps and SSO-oriented expansion.

### Result Summary

- Preserved the existing ThePeach repository structure and implemented the requested platform work in-place without introducing the conflicting `apps/` or `config/` tree.
- Extended the central `accounts` user model with `full_name` while keeping `display_name` as a compatibility field, and updated signup/login/profile payloads and JWT claims accordingly.
- Added auth helper pages for signup, login, logout, and a JWT-backed portal home using server-rendered templates that call the centralized `/api/v1/auth/` APIs directly.
- Added explicit signup-attempt logging while preserving existing signup/login/logout/token auth event logging and centralized logout revocation.
- Implemented `RegisteredApplication`, `RegisteredScreen`, and `RegisteredFeature` under `services` with company ownership, scoped uniqueness, active/inactive policy enforcement, and auditable create/update/deactivate behavior.
- Exposed registry APIs under `/api/v1/services/applications/`, `/api/v1/services/screens/`, and `/api/v1/services/features/`, plus soft-deactivate actions.
- Added operator-facing application registry templates for list/create/edit flows.
- Added `/api/v1/gateway/health/` and `/api/v1/gateway/integrations/applications/` as machine-readable gateway-facing integration surfaces for future trusted app and SSO expansion.
- Tightened fallback API error rendering so serializer validation failures also use the standardized ThePeach error envelope with `code`, `message`, and `details`.
- Added concise documentation for centralized auth, application registry governance, current JWT-based SSO direction, and future OAuth2/OIDC expansion path.

### Notable Files Changed

- `accounts/models.py`
- `accounts/admin.py`
- `accounts/serializers.py`
- `accounts/services.py`
- `accounts/views.py`
- `accounts/web_views.py`
- `accounts/web_urls.py`
- `accounts/tests.py`
- `accounts/migrations/0003_user_full_name.py`
- `services/models.py`
- `services/serializers.py`
- `services/registry_service.py`
- `services/api_views.py`
- `services/views.py`
- `services/urls.py`
- `services/web_views.py`
- `services/web_urls.py`
- `services/admin.py`
- `services/tests.py`
- `services/migrations/0003_alter_serviceregistry_company_registeredapplication_and_more.py`
- `gateway/services.py`
- `gateway/views.py`
- `gateway/urls.py`
- `gateway/tests.py`
- `common/renderers.py`
- `common/tests.py`
- `project/urls.py`
- `templates/base.html`
- `templates/accounts/signup.html`
- `templates/accounts/login.html`
- `templates/accounts/logout.html`
- `templates/accounts/home.html`
- `templates/services/applications/list.html`
- `templates/services/applications/form.html`
- `docs/central-auth-and-application-registry.md`

### Validation Status

- `python manage.py makemigrations accounts services`: passed
- `python manage.py migrate`: passed
- `python manage.py check`: passed
- `env DJANGO_SETTINGS_MODULE=project.settings.prod DJANGO_DEBUG=false DJANGO_SECRET_KEY=prod-secret DJANGO_ALLOWED_HOSTS=thesysm.com,.thesysm.com DJANGO_CSRF_TRUSTED_ORIGINS=https://thesysm.com,https://app.thesysm.com DJANGO_LOG_DIR=/tmp/thepeach-prod-logs python manage.py check`: passed
- `python manage.py test accounts.tests services.tests gateway.tests common.tests`: passed

## 2026-03-18 12:37:48 KST

### User Request Summary

- Save the previously produced `# ThePeach Architecture Audit Report` into Codex history.

### Implementation/Result Summary

- Appended the repository architecture audit result into the monthly Codex history file.
- Recorded the audit verdict as `PARTIAL PASS`.
- Preserved the major audit conclusions:
  - platform direction is real and coherent in code
  - auth ownership, API versioning, gateway contract, and logging foundations exist
  - production readiness is the primary weak area
  - missing `project/settings/prod.py` and insecure base settings were identified as critical violations
  - file-based production logging and proxy/security hardening were identified as required fixes
- No repository source code was modified as part of this step beyond this history append.

### Notable Files Changed

- `codex-history/2026-03.md`

### Validation Status

- History entry appended successfully.
- No code changes or runtime validation were required for this archival step.

## 2026-03-18 12:55:54 KST

### User Request Summary

- Apply a production-oriented remediation pass based on the ThePeach architecture audit.
- Fix production blockers first, then correct route discipline, operational logging, gateway contract behavior, and coupling issues.

### Implementation/Result Summary

- Added `project/settings/prod.py` and refactored `base.py` and `local.py` to separate safe local defaults from production-hardening behavior.
- Removed insecure base defaults for wildcard hosts and production secret handling, while preserving local development usability.
- Added file-compatible logging handlers and log directory support.
- Made DB-backed audit logging failure-safe so persistence failures do not mask request failures.
- Canonicalized auth routing to `/api/v1/auth/` and removed the legacy `/api/v1/accounts/` gateway alias.
- Added explicit failed-login audit logging.
- Tightened gateway unresolved target behavior to return `404` with the standard error contract.
- Reduced serializer-level cross-app coupling by replacing `CompanySerializer` imports with local explicit company references.
- Removed ambiguous or unused surface files (`common/permissions.py`, `logs/views.py`).
- Added production/deployment documentation and a minimal requirements manifest.

### Notable Files Changed

- `project/settings/base.py`
- `project/settings/local.py`
- `project/settings/prod.py`
- `logs/services.py`
- `common/middleware.py`
- `accounts/services.py`
- `gateway/services.py`
- `gateway/views.py`
- `gateway/urls.py`
- `media/serializers.py`
- `services/serializers.py`
- `accounts/tests.py`
- `gateway/tests.py`
- `logs/tests.py`
- `docs/production-deployment.md`
- `requirements/base.txt`
- `codex-history/2026-03.md`

### Validation Status

- `DJANGO_SETTINGS_MODULE=project.settings.local python manage.py check`: passed
- `DJANGO_SETTINGS_MODULE=project.settings.prod ... python manage.py check`: passed
- `python manage.py test`: passed

## 2026-03-18 11:44:45 KST

### User Request Summary

- Refactor code into ThePeach architecture with better separation of concerns, API consistency, and maintainability.

### Result Summary

- Refactored `accounts`, `media`, and `gateway` so views are closer to request/response orchestration only.
- Expanded `accounts/services.py` into an application-service boundary for signup, login, refresh, logout, and auth manifest construction.
- Added `media/services.py` so company-scoped query building and department-based write checks are no longer embedded in views.
- Moved gateway resolution orchestration and logging fully into `gateway/services.py`, leaving `gateway/views.py` as a thin API layer.
- Preserved the existing API contract and route surface while reducing mixed concerns in view classes.

### Notable Files Changed

- `accounts/services.py`
- `accounts/views.py`
- `media/services.py`
- `media/views.py`
- `gateway/services.py`
- `gateway/views.py`

### Validation Status

- `python manage.py check`: passed
- `python manage.py test`: passed

## 2026-03-18 11:40:54 KST

### User Request Summary

- Design and implement the API Gateway as the unified API entry, routing layer, integration layer, logging-aware edge, and MCP-compatible interface.

### Result Summary

- Expanded `gateway` from a simple API root into a real gateway contract layer.
- Added `gateway/services.py` to centralize route catalog generation and service target resolution without putting business logic into views.
- Added `gateway/serializers.py` to validate gateway resolution requests deterministically.
- Implemented a gateway manifest endpoint, route catalog endpoint, and route resolution endpoint under `/api/v1/gateway/`.
- Kept response standardization aligned with existing ThePeach response helpers in `common`.
- Reused request logging and system logging through existing middleware and `logs.services`.
- Integrated service discovery with company-scoped `ServiceRegistry` data so gateway resolution respects tenant boundaries.
- Preserved loose coupling by making `gateway` depend on `services` metadata rather than service implementation internals.

### Notable Files Changed

- `gateway/serializers.py`
- `gateway/services.py`
- `gateway/views.py`
- `gateway/urls.py`
- `gateway/tests.py`

### Validation Status

- `python manage.py check`: passed
- `python manage.py test`: passed

## 2026-03-19 01:05:00 KST

### User Request Summary

- Prepare ThePeach production deployment environment using the existing movies/nginx/cloudflared host conventions.

### Result Summary

- Inspected the existing `movies` Gunicorn unit/socket/config, active nginx Django host config, and active cloudflared tunnel config.
- Added source-controlled ThePeach deployment assets under `deploy/production/` for Gunicorn, nginx, Cloudflare ingress, env example, installer, and validator scripts.
- Chose a socket-based deployment shape aligned with the host convention:
  - socket: `/run/gunicorn_thepeach.sock`
  - hostname: `thepeach.thesysm.com`
  - nginx aliases: `/var/www/thepeach/static` and `/var/www/thepeach/media`
  - logs: `/logs/thepeach`
- Updated production deployment documentation with concrete installed paths and helper script references.
- Verified the Gunicorn unit files with `systemd-analyze verify` and verified Django production settings load with `project.settings.prod`.
- Could not complete privileged installation into `/etc` and service restart from this session because `sudo` on this host requires an interactive password prompt.

### Notable Files Changed

- `deploy/production/README.md`
- `deploy/production/gunicorn_thepeach.service`
- `deploy/production/gunicorn_thepeach.socket`
- `deploy/production/gunicorn_thepeach.config.py`
- `deploy/production/nginx_thepeach.conf`
- `deploy/production/cloudflared_thepeach_ingress.yml`
- `deploy/production/thepeach.env.example`
- `deploy/production/install_thepeach.sh`
- `deploy/production/validate_thepeach.sh`
- `docs/production-deployment.md`

### Validation Status

- `systemd-analyze verify deploy/production/gunicorn_thepeach.socket deploy/production/gunicorn_thepeach.service`: passed
- `DJANGO_SETTINGS_MODULE=project.settings.prod ... python manage.py check`: passed
- privileged installation to `/etc`, nginx reload, cloudflared reload, and systemd enable/start: blocked by interactive `sudo` password requirement

## 2026-03-19 01:22:00 KST

### User Request Summary

- Package the remaining manual ThePeach production deployment steps into a single runnable batch-style script.

### Result Summary

- Added a one-shot deployment runner that:
  - regenerates a shell-safe production secret
  - rewrites `/etc/thepeach/thepeach.env`
  - calls the installer
  - calls the validator
- Updated deployment docs and deployment README to point operators at the new single-command flow.

### Notable Files Changed

- `deploy/production/run_thepeach_deploy.sh`
- `deploy/production/README.md`
- `docs/production-deployment.md`

### Validation Status

- script structure reviewed locally
- execution still requires interactive `sudo` on the host

## 2026-03-19 02:06:00 KST

### User Request Summary

- Diagnose external ThePeach HTTPS access after DNS and tunnel were working but `/api/v1/health/` was redirecting to itself.

### Result Summary

- Confirmed Cloudflare DNS and tunnel were active.
- Confirmed Gunicorn and nginx were serving locally.
- Traced the redirect loop to nginx proxy headers:
  - `/etc/nginx/proxy_params` sets `X-Forwarded-Proto $scheme`
  - cloudflared reaches local nginx over plain HTTP
  - Django therefore kept redirecting to HTTPS
- Updated the source-controlled ThePeach nginx site config to override `X-Forwarded-Proto https` and pass explicit host headers for Cloudflare tunnel traffic.

### Notable Files Changed

- `deploy/production/nginx_thepeach.conf`
- `docs/production-deployment.md`

### Validation Status

- root cause identified from live host config
- source-controlled nginx fix prepared

## 2026-03-20 00:46:50 KST

### User Request Summary

- Create a new Django project-based service inside `ThePeach` that does not implement its own login system.
- Make the new service trust ThePeach `/api/v1/auth/` APIs as the authentication source of truth.
- Provide reusable auth client/authentication wrapping, a minimal authenticated example endpoint or view, configuration, docs, tests, and an append-only history update.

### Result Summary

- Added a separate Django service at `trusted_portal/` using the repository's `settings/base.py` and `settings/local.py` convention.
- Implemented `ThePeachAuthClient` to call ThePeach `GET /api/v1/auth/me/` and normalize the remote user payload into a request-local `ThePeachRemoteUser`.
- Chose a DRF authentication class as the primary integration boundary because the new service exposes API endpoints and DRF permission handling already matches the repository's API-first patterns.
- Added companion middleware so non-DRF Django views can read the same remote auth context without creating a local auth authority or syncing users into a DB table.
- Added a minimal authenticated example endpoint at `/api/v1/example/auth-status/` and a plain Django root view at `/` to show the normalized auth context.
- Added timeout / 401 / 403 / 5xx / contract-error handling for upstream auth failures.
- Documented the service, environment variables, execution flow, and design rationale in `trusted_portal/README.md` and `docs/trusted-portal-service.md`.

### Notable Files Changed

- `trusted_portal/manage.py`
- `trusted_portal/config/urls.py`
- `trusted_portal/config/settings/base.py`
- `trusted_portal/config/settings/local.py`
- `trusted_portal/core/auth/client.py`
- `trusted_portal/core/auth/authentication.py`
- `trusted_portal/core/auth/middleware.py`
- `trusted_portal/core/auth/models.py`
- `trusted_portal/core/auth/permissions.py`
- `trusted_portal/core/views.py`
- `trusted_portal/core/tests.py`
- `trusted_portal/.env.example`
- `trusted_portal/README.md`
- `docs/trusted-portal-service.md`
- `README.md`
- `codex-history/2026-03.md`

### Validation Status

- `cd trusted_portal && python manage.py check`: passed
- `cd trusted_portal && python manage.py test`: passed

## 2026-03-20 09:48:59 KST

### User Request Summary

- Refactor ThePeach into a production-ready public/internal split architecture.
- Keep public APIs available while isolating admin, internal auth, and internal gateway operations.
- Preserve centralized authentication in `accounts`, preserve gateway as the integration layer, and update deployment/docs accordingly.

### Result Summary

- Added reusable internal access enforcement in `common` with host-based and optional header-based checks, middleware protection for `/admin/` and internal API prefixes, and security logging for allowed/denied access attempts.
- Split `accounts` into public and internal API modules so normal JWT flows remain under `/api/v1/auth/...` while internal auth operations now live under `/api/v1/auth/internal/...`.
- Split `gateway` into public and internal API modules so public discovery and application integrations remain public-facing while route catalog, route resolve, and tool endpoints are isolated under internal paths.
- Updated settings to support public and internal hostnames, env-driven internal-access headers, and filesystem-backed `security.log`.
- Added dedicated public/internal Nginx templates, updated production environment examples and deployment docs, and documented route classification, Cloudflare Access integration points, rollout, smoke checks, and rollback guidance.
- Added targeted tests for internal host restrictions, header-gated internal access, and admin blocking on the public host.

### Notable Files Changed

- `common/internal_access.py`
- `common/permissions.py`
- `common/middleware.py`
- `accounts/api/public_views.py`
- `accounts/api/internal_views.py`
- `accounts/public_urls.py`
- `accounts/internal_urls.py`
- `accounts/services.py`
- `accounts/urls.py`
- `gateway/api/public_views.py`
- `gateway/api/internal_views.py`
- `gateway/services.py`
- `gateway/urls.py`
- `project/settings/base.py`
- `project/settings/prod.py`
- `deploy/production/nginx_thepeach_public.conf`
- `deploy/production/nginx_thepeach_internal.conf`
- `deploy/production/thepeach.env.example`
- `docs/public-internal-split.md`
- `docs/production-deployment.md`
- `common/docs_registry.py`
- `accounts/tests.py`
- `gateway/tests.py`
- `common/tests.py`
- `codex-history/2026-03.md`

### Validation Status

- `python manage.py check`: passed
- `bash -n deploy/production/gunicornctl.sh`: passed
- `bash -n deploy/production/install_thepeach.sh`: passed
- `bash -n deploy/production/validate_thepeach.sh`: passed
- `bash -n deploy/production/run_thepeach_deploy.sh`: passed
- Django setup import smoke for new modules: passed
- Django URL resolve smoke for new public/internal routes: passed
- `python manage.py test accounts.tests gateway.tests common.tests`: blocked by sandbox PostgreSQL socket restriction (`/var/run/postgresql/.s.PGSQL.5432` access denied)

## 2026-03-20 09:25:55 KST

### User Request Summary

- Change Gunicorn startup, restart, and stop operations so they run as `cskang` without requiring `sudo` or root for the Gunicorn lifecycle itself.

### Result Summary

- Reworked the production Gunicorn deployment from a root-managed system unit plus socket activation into a user-scoped `systemd` service for `cskang`.
- Changed Gunicorn binding from the root-owned unix socket path to `127.0.0.1:8001`, which removes the `/run` permission dependency and avoids group-sharing issues because `cskang` is not in `www-data`.
- Updated the deployment installer and validator so Gunicorn is installed and controlled through `systemctl --user`, while Nginx and Cloudflared keep their existing root-managed roles.
- Updated the Nginx and Cloudflared deployment assets to route through local Nginx and local Gunicorn over loopback TCP.
- Added a small `deploy/production/gunicornctl.sh` helper for `start|stop|restart|status|logs` as the `cskang` user.
- Removed the now-obsolete `deploy/production/gunicorn_thepeach.socket` asset and refreshed production deployment documentation to match the new control path.

### Notable Files Changed

- `deploy/production/gunicorn_thepeach.service`
- `deploy/production/gunicorn_thepeach.config.py`
- `deploy/production/install_thepeach.sh`
- `deploy/production/validate_thepeach.sh`
- `deploy/production/nginx_thepeach.conf`
- `deploy/production/cloudflared_thepeach_ingress.yml`
- `deploy/production/gunicornctl.sh`
- `deploy/production/README.md`
- `docs/production-deployment.md`
- `deploy/production/gunicorn_thepeach.socket` (deleted)
- `codex-history/2026-03.md`

### Validation Status

- `bash -n deploy/production/install_thepeach.sh`: passed
- `bash -n deploy/production/validate_thepeach.sh`: passed
- `bash -n deploy/production/run_thepeach_deploy.sh`: passed
- `bash -n deploy/production/gunicornctl.sh`: passed
- `systemd-analyze verify deploy/production/gunicorn_thepeach.service`: passed

## 2026-03-20 01:53:45 KST

### User Request Summary

- Save login debugging logs for `trusted_portal`.
- Use the logged error content to determine why the current login is failing.

### Result Summary

- Added persistent auth relay debug logging to `trusted_portal` at `.runtime/logs/auth-debug.log`.
- Logged upstream login candidates, status codes, response snippets, and upstream request IDs without logging raw passwords.
- Reproduced a safe failing login attempt against the live ThePeach production auth endpoint and captured the actual upstream error in the saved log.
- Determined that the current failure is not a normal invalid-credential response for the user's reported case. The saved log captured `HTTP 403` with body `error code: 1010`, which indicates ThePeach's Cloudflare edge is denying the server-side login relay path before normal app-level authentication completes.
- Updated login error classification so this edge-block case surfaces a more accurate message instead of the generic credential-rejection text.

### Notable Files Changed

- `trusted_portal/config/settings/base.py`
- `trusted_portal/.env.example`
- `trusted_portal/core/auth/client.py`
- `trusted_portal/core/tests.py`
- `trusted_portal/README.md`
- `codex-history/2026-03.md`

### Validation Status

- `cd trusted_portal && python manage.py check`: passed
- `cd trusted_portal && python manage.py test`: passed
- safe live failing login relay probe against `https://thepeach.thesysm.com/api/v1/auth/login/`: logged and inspected

## 2026-03-20 01:41:14 KST

### User Request Summary

- Clarify the login failure after confirming the same account can log in on the real ThePeach site.

### Result Summary

- Re-checked the production auth manifest and live `POST /api/v1/auth/login/` response shape against `https://thepeach.thesysm.com`.
- Adjusted the `trusted_portal` login page to explicitly prioritize the canonical ThePeach login page.
- Added a direct link to `https://thepeach.thesysm.com/auth/login/` and updated the failure copy to explain that server-side credential relay can behave differently from direct browser login in production.
- Kept the existing upstream-backed relay path available for local/testing scenarios, but made the UX point users at the canonical upstream login first.

### Notable Files Changed

- `trusted_portal/core/templates/core/login.html`
- `trusted_portal/core/static/core/site.css`
- `trusted_portal/core/tests.py`
- `codex-history/2026-03.md`

### Validation Status

- `cd trusted_portal && python manage.py check`: passed
- `cd trusted_portal && python manage.py test`: passed

## 2026-03-20 01:33:20 KST

### User Request Summary

- Correct the assumed ThePeach upstream URL.
- Use `https://thepeach.thesysm.com/` as the actual ThePeach base URL.

### Result Summary

- Updated `trusted_portal` default upstream configuration to `https://thepeach.thesysm.com`.
- Updated the example environment file so local overrides are explicit and the production-like default is the real ThePeach URL.
- Updated service documentation to state the correct ThePeach base URL and clarify that local fallback URLs are optional development aids only.

### Notable Files Changed

- `trusted_portal/config/settings/base.py`
- `trusted_portal/.env.example`
- `trusted_portal/README.md`
- `docs/trusted-portal-service.md`
- `codex-history/2026-03.md`

### Validation Status

- `cd trusted_portal && python manage.py check`: passed

## 2026-03-20 01:30:57 KST

### User Request Summary

- Fix the login flow after the browser showed `Unexpected response from ThePeach auth service: HTTP 404.`

### Result Summary

- Identified that local development could point `trusted_portal` back at itself when resolving ThePeach auth endpoints.
- Updated the reusable ThePeach auth client to try configured fallback base URLs and to auto-try the opposite local dev port (`8000` <-> `8001`) when the current request host is local and the first upstream lookup returns `404`.
- Wired the request host through login, middleware auth hydration, and DRF auth resolution so both login and `/api/v1/auth/me/` can recover from same-host local misrouting.
- Added a regression test covering the `404` fallback case.

### Notable Files Changed

- `trusted_portal/config/settings/base.py`
- `trusted_portal/.env.example`
- `trusted_portal/core/auth/client.py`
- `trusted_portal/core/auth/authentication.py`
- `trusted_portal/core/auth/middleware.py`
- `trusted_portal/core/views.py`
- `trusted_portal/core/tests.py`
- `codex-history/2026-03.md`

### Validation Status

- `cd trusted_portal && python manage.py check`: passed
- `cd trusted_portal && python manage.py test`: passed

## 2026-03-20 01:24:30 KST

### User Request Summary

- Add a login screen to `trusted_portal`.

### Result Summary

- Added a browser login page at `trusted_portal/auth/login/`.
- Kept authentication ownership in ThePeach by sending submitted credentials to upstream `POST /api/v1/auth/login/` through the reusable `ThePeachAuthClient`.
- Added a minimal session wrapper endpoint that stores only the upstream-issued access token in the configured session key for later request authentication.
- Updated navigation links so the login screen is reachable from the homepage and auth pages.
- Added tests for login page rendering, session login success, missing-credentials validation, and upstream invalid-credential handling.

### Notable Files Changed

- `trusted_portal/config/settings/base.py`
- `trusted_portal/.env.example`
- `trusted_portal/core/auth/client.py`
- `trusted_portal/core/urls.py`
- `trusted_portal/core/views.py`
- `trusted_portal/core/templates/core/login.html`
- `trusted_portal/core/templates/core/home.html`
- `trusted_portal/core/templates/core/auth_me.html`
- `trusted_portal/core/static/core/site.css`
- `trusted_portal/README.md`
- `trusted_portal/core/tests.py`
- `codex-history/2026-03.md`

### Validation Status

- `cd trusted_portal && python manage.py check`: passed
- `cd trusted_portal && python manage.py test`: passed

## 2026-03-20 01:19:15 KST

### User Request Summary

- Make `/api/v1/auth/me/` render properly in the browser instead of only behaving like an authenticated API route.

### Result Summary

- Updated `trusted_portal` so `/api/v1/auth/me/` now renders an HTML compatibility page for browser requests.
- Kept JSON behavior for API clients and returned a standardized `401 not_authenticated` response when JSON clients omit a token.
- Added an unauthenticated browser state that explains how the route works and why no local login happens inside the service.
- Expanded tests to cover authenticated HTML, authenticated JSON, unauthenticated HTML, and unauthenticated JSON behavior.

### Notable Files Changed

- `trusted_portal/core/responses.py`
- `trusted_portal/core/views.py`
- `trusted_portal/core/templates/core/auth_me.html`
- `trusted_portal/core/tests.py`
- `codex-history/2026-03.md`

### Validation Status

- `cd trusted_portal && python manage.py check`: passed
- `cd trusted_portal && python manage.py test`: passed

## 2026-03-20 01:17:48 KST

### User Request Summary

- Fix the missing `trusted_portal` route at `/api/v1/auth/me/`.
- Make that page exist properly instead of returning a 404.

### Result Summary

- Added a compatibility `AuthMeAPIView` at `/api/v1/auth/me/` inside `trusted_portal`.
- Kept ThePeach as the auth source of truth by serving the normalized remote user context from the existing upstream-backed auth integration instead of inventing local auth state.
- Added HTML rendering for browser requests and JSON rendering for API requests on the same route.
- Updated the homepage navbar to link to the new local `Auth Me` route.
- Added tests covering both the HTML and JSON behaviors of `/api/v1/auth/me/`.

### Notable Files Changed

- `trusted_portal/core/urls.py`
- `trusted_portal/core/views.py`
- `trusted_portal/core/templates/core/home.html`
- `trusted_portal/core/templates/core/auth_me.html`
- `trusted_portal/core/static/core/site.css`
- `trusted_portal/core/tests.py`
- `codex-history/2026-03.md`

### Validation Status

- `cd trusted_portal && python manage.py check`: passed
- `cd trusted_portal && python manage.py test`: passed

## 2026-03-20 01:12:55 KST

### User Request Summary

- Add a real homepage to `trusted_portal/`.
- Include a navigation bar.

### Result Summary

- Replaced the JSON root response in `trusted_portal` with a rendered landing page template.
- Added a navigation bar with links to the homepage, authenticated example API, and ThePeach auth endpoint.
- Added dedicated stylesheet assets for the new homepage and kept the current authenticated user state visible on the page.
- Updated tests so the homepage render path validates the navbar and authenticated user rendering.

### Notable Files Changed

- `trusted_portal/core/views.py`
- `trusted_portal/core/templates/core/home.html`
- `trusted_portal/core/static/core/site.css`
- `trusted_portal/core/tests.py`
- `codex-history/2026-03.md`

### Validation Status

- `cd trusted_portal && python manage.py check`: passed
- `cd trusted_portal && python manage.py test`: passed
